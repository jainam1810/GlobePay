// Retired: invoice OCR is superseded by the AI freelancer importer in the
// operator console (client detail → "Import list with AI").
import { redirect } from "next/navigation";
export default function LegacyInvoices() { redirect("/route"); }
